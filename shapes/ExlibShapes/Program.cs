using System;

// exlib-shapes render|schematic|tree|measure ... - see the tool's README once T8 adds the
// subcommand dispatch. Until then the tool builds and tests as a library exercised in-process by
// ExlibShapes.Tests.
Console.Error.WriteLine("exlib-shapes: no commands yet");
return 2;
